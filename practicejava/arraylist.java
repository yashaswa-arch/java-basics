//most usewd methods

// add(value)	Adds element at end
// add(index, val)	Inserts at position
// get(index)	Returns value at index
// set(index, val)	Updates value at index
// remove(index)	Deletes value at index
// size()	Returns current length of list
// clear()	Deletes all elements
// contains(val)	Checks if element exists

import java.util.ArrayList;

public class arraylist {
    public static void main(String[] args) {
        // Step 1: Create an ArrayList of Integers
        ArrayList<Integer> list = new ArrayList<>();

        // // Step 2: Add elements
        // list.add(10);
        // list.add(20);
        // list.add(30);

        // // Step 3: Access elements
        // System.out.println("First element: " + list.get(0));  // 10

        // // Step 4: Remove by index
        // list.remove(1);  // removes 20

        // // Step 5: Print all values
        // for (int val : list) {
        //     System.out.print(val + " ");
        // }

        // // Step 6: Check size
        // System.out.println("\nSize: " + list.size());

        //adding even numbers

// for (int i = 1; i <= 10; i++) {
//     if (i % 2 == 0) {
//         list.add(i);
//     }
// }

// System.out.println("Even Numbers: " + list);

    }
}
